# 🎤 Whisper Quick-Type — голос в текст и запись встреч

Голосовой ввод в любое поле Windows + запись экрана со звуком и расшифровка диалогов.

---

## ⚡ Быстрый старт

**Запуск:** дважды кликните **`🚀 ЗАПУСТИТЬ.bat`**

При первом запуске установятся Python (если нет), библиотеки и Whisper.  
Первый раз — 5–10 минут, дальше — запуск за пару секунд.

После установки на рабочем столе появится ярлык. Приложение уходит в трей (рядом с часами), консоль не показывается.

---

## ⌨️ Горячие клавиши

| Действие | Комбинация |
|----------|------------|
| **Голос → текст** | Зажмите **`CTRL + Z + X`** (по умолчанию), говорите, отпустите — текст вставится в поле, где стоит курсор |
| **Запись экрана** | **`CTRL + ALT + Print Screen`** — старт/стоп записи всего экрана с микрофоном |

Комбинацию для голоса можно сменить во вкладке **«Голос»** (мод + две клавиши, кнопка OK).

---

## 📑 Вкладки приложения

### 🎙️ Голос

- **Горячие клавиши** — настройка комбинации для голосового ввода  
- **Модель Whisper** — tiny / base / medium (точность и скорость)  
- **Микрофон** — выбор устройства записи  
- **Показать лог** — включить/выключить панель лога  
- **Автозапуск с Windows** — включить/выключить  
- **В трей** / **Выход**

Как пользоваться: поставьте курсор в любое текстовое поле (чат, документ, форма), зажмите выбранную комбинацию, говорите, отпустите — распознанный текст вставится автоматически.

### 📹 Встречи

- **НАЧАТЬ ЗАПИСЬ** — запись всего экрана + микрофон в один MP4  
- **СТОП** — остановка и сохранение в папку `temp_records`  
- **Список записей** — выбор сохранённого видео  
- **РАСШИФРОВАТЬ** — расшифровка выбранного видео в текст (по паузам, «в столбик»)  
- **Папка** — открыть папку с записями  

Удобно: **Ctrl+Alt+Print Screen** — быстрый старт/стоп записи без открытия окна.

---

## 📁 Структура проекта

| Файл / папка | Назначение |
|--------------|------------|
| `🚀 ЗАПУСТИТЬ.bat` | Первый запуск: установка, далее — запуск приложения |
| `main.py` | Главное приложение (голос + встречи) |
| `recorder.py` | Запись голоса с микрофона |
| `meeting_recorder.py` | Запись экрана и микрофона в MP4 |
| `meeting_transcriber.py` | Расшифровка записи встречи |
| `transcriber.py` | Распознавание речи (Whisper) |
| `hotkeys.py` | Обработка горячих клавиш |
| `utils.py` | Настройки, модели, автозапуск |
| `temp_records/` | Папка записей встреч (создаётся при первой записи) |

---

## 🔧 Требования

- **Windows 10/11**  
- **Python 3.10+** (ставится автоматически при первом запуске bat-файла)  
- Микрофон  
- Для расшифровки встреч при первом использовании скачается модель Whisper (medium)

---

## ❓ Частые вопросы

**Текст не вставляется после голоса**  
Проверьте, что курсор в текстовом поле, подождите ~1 сек после отпускания клавиш. В настройках выберите нужный микрофон и при необходимости модель base или medium.

**Запись встречи не стартует**  
Убедитесь, что во вкладке «Встречи» есть кнопки записи (модуль встреч загружен). При первом запуске могут подтянуться зависимости (mss, opencv, imageio-ffmpeg).

**Где лежат записи встреч**  
В папке `temp_records` в каталоге приложения. Кнопка **«Папка»** во вкладке «Встречи» открывает её.

**Как сменить горячие клавиши для голоса**  
Вкладка «Голос» → выберите мод и две клавиши → нажмите OK.

---

*Whisper Quick-Type — голос в текст и запись встреч в одном приложении.*
